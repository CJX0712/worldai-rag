# ADR-0003: llama.cpp CPU 线程数锁定 2-4

状态：已接受 · 作者：晨星

## 背景

llama.cpp 默认线程数为 cpu_count-1。实测小量化模型（1.5B q4_k_m）在
CPU 上的瓶颈是**内存带宽**而非算力：线程超过 4 后吞吐不升反降，
默认配置下约慢 4 倍。

## 决策

`WORLDAI_LLM_THREADS` 默认 3，可调范围 2-4；文档明确说明原因。

## 后果

- 好：开箱即接近最优吞吐（实测 ~15-25 tok/s）。
- 坏：不同机型最优值略异；保留环境变量可调。
